#include "main.h"

/**
 * _abs - returns the absolute value of an integer
 * @n: Placeholder for the iinteger
 * Return: Returns the absolute value of the integer
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
