#include "main.h"

/**
 * _islower - checks for lowercase character
 * @c: The character placeholder
 * Return: 1 if c is lowercase amd 0 if not
 */
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
