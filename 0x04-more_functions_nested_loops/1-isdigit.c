#include "main.h"
/**
 * _isdigit - Checks for a digit between 0 to 9
 * @c: integer placeholder
 * Return: 1 if c is a digit between 0 and 9 and zero if otherwise
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
