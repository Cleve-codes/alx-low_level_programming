#include <stdio.h>

/**
 * main - Prints alphabet lowercase letters
 * Return: Always reuurn 0 if succesdful
 */
int main(void)
{
	char ch = 'a';

	while (ch <= 'z')
	{
		if (ch != 'e' && ch != 'q')
		{
			putchar(ch);
		}
		ch++;
	}
	putchar('\n');
	return (0);
}
