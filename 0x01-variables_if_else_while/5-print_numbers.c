#include <stdio.h>

/**
 * main - Prints all numbers from 0 to 10
 * Return: Always 0 if succesful
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		printf("%d", n);
	printf("\n");
	return (0);
}
